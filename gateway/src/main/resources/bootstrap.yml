spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://${CONGIG_SERVER_HOST:localhost}:8071


logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.optimagrowth: DEBUG